import React from "react";

export default function PrimaryButton() {
  return (
    <div
      style={{
        backgroundColor: "blue",
        borderRadius:"20px",
        
      }}
      onClick={() => {}}
    >
      PrimaryButton
    </div>
  );
}
